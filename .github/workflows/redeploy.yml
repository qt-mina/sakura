name: Bot Health Check & Auto-Redeploy

on:
  schedule:
    - cron: '0,10,20,30,40,50 * * * *'  # Runs every 10 minutes
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check bot health (Real Functionality Test)
        id: health
        run: |
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          CHECK_TIME=$(date '+%H:%M UTC')
          
          # Step 1: Check if bot token is valid
          TOKEN_CHECK=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/getMe")
          
          # Step 2: Get recent updates to verify bot is processing messages
          UPDATES_RESPONSE=$(curl -s "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/getUpdates?limit=1&timeout=5")
          
          # Step 3: Send an informative test message and verify it's sent
          TEST_MSG_RESPONSE=$(curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=✅ <b>Bot Health Check</b>%0A%0A🕒 Time: $CHECK_TIME%0A📊 Status: <b>Operational</b>%0A🔄 API: Responding%0A✉️ Messages: Working%0A%0A⏭ Next check in 10 minutes" \
            -d "parse_mode=HTML" \
            -d "disable_notification=true")
          
          # Parse responses
          UPDATES_OK=$(echo "$UPDATES_RESPONSE" | grep -o '"ok":true' || echo "")
          TEST_MSG_OK=$(echo "$TEST_MSG_RESPONSE" | grep -o '"ok":true' || echo "")
          
          # Determine health status
          if [ "$TOKEN_CHECK" = "200" ] && [ -n "$UPDATES_OK" ] && [ -n "$TEST_MSG_OK" ]; then
            echo "status=healthy" >> $GITHUB_OUTPUT
            echo "message=✅ Bot Status: HEALTHY%0A🕒 Time: $TIMESTAMP%0A📊 Token: Valid%0A📨 API: Responding%0A✉️ Messages: Sendable" >> $GITHUB_OUTPUT
          else
            echo "status=down" >> $GITHUB_OUTPUT
            echo "message=❌ <b>Bot Status: DOWN</b>%0A%0A🕒 Time: $TIMESTAMP%0A📊 Token HTTP: $TOKEN_CHECK%0A📨 Updates API: $([ -n "$UPDATES_OK" ] && echo "OK" || echo "FAILED")%0A✉️ Send Test: $([ -n "$TEST_MSG_OK" ] && echo "OK" || echo "FAILED")%0A%0A⚠️ Initiating recovery process..." >> $GITHUB_OUTPUT
          fi

      - name: Send critical status alert
        if: steps.health.outputs.status == 'down'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=${{ steps.health.outputs.message }}" \
            -d "parse_mode=HTML" || true

      - name: Wait before redeploying
        if: steps.health.outputs.status == 'down'
        run: sleep 30

      - name: Recheck bot health
        id: recheck
        if: steps.health.outputs.status == 'down'
        run: |
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          CHECK_TIME=$(date '+%H:%M UTC')
          
          TOKEN_CHECK=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/getMe")
          
          UPDATES_RESPONSE=$(curl -s "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/getUpdates?limit=1&timeout=5")
          
          TEST_MSG_RESPONSE=$(curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=🔍 <b>Recheck Ping</b> - $CHECK_TIME" \
            -d "parse_mode=HTML" \
            -d "disable_notification=true")
          
          UPDATES_OK=$(echo "$UPDATES_RESPONSE" | grep -o '"ok":true' || echo "")
          TEST_MSG_OK=$(echo "$TEST_MSG_RESPONSE" | grep -o '"ok":true' || echo "")
          
          if [ "$TOKEN_CHECK" = "200" ] && [ -n "$UPDATES_OK" ] && [ -n "$TEST_MSG_OK" ]; then
            echo "status=healthy" >> $GITHUB_OUTPUT
            echo "message=✅ <b>Bot Status: RECOVERED</b>%0A%0A🕒 Time: $TIMESTAMP%0A💡 Bot is back online%0A🎯 No redeploy needed%0A%0A✨ Resuming normal monitoring" >> $GITHUB_OUTPUT
          else
            echo "status=down" >> $GITHUB_OUTPUT
            echo "message=❌ <b>Bot Status: STILL DOWN</b>%0A%0A🕒 Time: $TIMESTAMP%0A🔄 Triggering automatic redeploy...%0A⏳ Please wait 2-3 minutes" >> $GITHUB_OUTPUT
          fi

      - name: Send recheck update
        if: steps.health.outputs.status == 'down'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=${{ steps.recheck.outputs.message }}" \
            -d "parse_mode=HTML" || true

      - name: Trigger Render Redeploy
        id: redeploy
        if: steps.health.outputs.status == 'down' && steps.recheck.outputs.status == 'down'
        run: |
          DEPLOY_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" -X POST ${{ secrets.REDEPLOY }})
          
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          
          if [ "$DEPLOY_RESPONSE" = "200" ] || [ "$DEPLOY_RESPONSE" = "201" ]; then
            echo "success=true" >> $GITHUB_OUTPUT
            echo "message=🔄 <b>Redeploy TRIGGERED</b>%0A%0A🕒 Time: $TIMESTAMP%0A📊 Deploy Response: $DEPLOY_RESPONSE%0A⏳ Bot restarting...%0A%0A✅ Expected online in 2-3 minutes" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
            echo "message=❌ <b>Redeploy FAILED</b>%0A%0A🕒 Time: $TIMESTAMP%0A📊 Deploy Response: $DEPLOY_RESPONSE%0A🚨 Manual intervention required!%0A%0A🔗 Check logs immediately" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Send redeploy status
        if: steps.health.outputs.status == 'down' && steps.recheck.outputs.status == 'down'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=${{ steps.redeploy.outputs.message }}" \
            -d "parse_mode=HTML" || true

      - name: Send workflow failure alert
        if: failure()
        run: |
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.CHAT_ID }}" \
            -d "text=🚨 <b>WORKFLOW ERROR</b>%0A%0A🕒 Time: $TIMESTAMP%0A⚠️ Health check workflow failed%0A🔗 Check: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}%0A%0A🛠 Action required!" \
            -d "parse_mode=HTML" || true